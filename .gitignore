bf-sms.keystore
signing.env
.gradle/
build/
app/build/
/local.properties

*.apk
*.keystore

# Documents de travail internes : ils décrivent l'arborescence de déploiement
# et les scripts internes, rien de tout ça n'a sa place dans un dépôt public.
HANDOFF.md
PLAN_ODOO_INBOX.md
README_INSTALLATION.md
.kotlin/
API_CONTRACT.md

# Bâti par tools/build-jssip.sh, jamais à la main.
tools/jssip/node_modules/

# ⚠️ Le trousseau de DÉBOGAGE du conteneur de fabrication (posé par
# `ANDROID_USER_HOME`). Il ne doit pas entrer au dépôt : c'est une clé, et deux
# trousseaux différents rendent les APK non installables l'une sur l'autre
# (« signatures do not match ») — BF #25765.
.android-debug/
